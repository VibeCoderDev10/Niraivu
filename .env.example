# NIRAIVU Environment Configuration
# Optional: Set API key if connecting to live streaming LLM endpoint
# If left blank, the application uses the built-in local NLP response engine.
VITE_AI_API_KEY=
VITE_AI_API_URL=
